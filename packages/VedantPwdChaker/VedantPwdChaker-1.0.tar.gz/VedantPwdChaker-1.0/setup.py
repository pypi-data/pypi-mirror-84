import setuptools

setuptools.setup(
    name="VedantPwdChaker",
    version=1.0,
    long_description="this package is for password cheaking.",
    packages=setuptools.find_packages(exclude=["tests", "data"])

)