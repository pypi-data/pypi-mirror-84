import setuptools
from pathlib import Path
setuptools.setup(name="gagandeep1pdf", version=1.0, description="My homepage",
                 packages=setuptools.find_packages(exclude=["tests", "data"]))
