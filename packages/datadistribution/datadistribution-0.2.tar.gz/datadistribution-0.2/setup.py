from setuptools import setup

setup(name = 'datadistribution',
      version = '0.2',
      description='Calculate mean and standard devitaion based on data file',
packages = ['datadistribution'],
zip_safe = False)
