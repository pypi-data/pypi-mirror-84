import setuptools

with open("README.md", "r") as fh:
    long_description = fh.read()

setuptools.setup(
    name="powerup",
    version="0.0.1",
    author="ambientnuance",
    author_email="ambientnuance@gmail.com",
    description="(name-squatting placeholder)",
    packages=setuptools.find_packages(),
)
