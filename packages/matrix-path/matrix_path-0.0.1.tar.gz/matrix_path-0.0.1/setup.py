from setuptools import setup

setup(
    name='matrix_path',
    version='0.0.1',
    description='Navigating and Visualizing paths in a 2D Matrix.',
    py_modules=["matrix"],
    package_dir={'': 'src'},
)
